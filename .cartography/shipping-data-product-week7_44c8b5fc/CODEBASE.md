# CODEBASE.md - Living Context

> [!NOTE]
> This file is generated by the Brownfield Cartographer. It contains a synthesized architectural context for AI agents.

## 🏗️ Architecture Overview
The system is composed of 26 modules across 4 business domains. The architecture is primarily driven by data transformations and utility-heavy hubs.

## 🚀 Critical Path (Top Hubs)
- `sqlalchemy.orm` (Importance: 0.0965)
- `telethon.tl.types` (Importance: 0.0571)
- `sqlalchemy.ext.declarative` (Importance: 0.0440)
- `docker-compose.yml` (Importance: 0.0309)
- `.github\workflows\ci.yml` (Importance: 0.0309)

## 🌊 Data Flow
### Entry Points (Sources)
`raw_image_detections`, `raw_telegram_messages`

### Exit Points (Sinks)
`dim_dates`, `dim_channels`, `valid_message_timestamps`, `positive_view_counts`, `fct_image_detections`

## ⚠️ Known Debt & Risks
### Documentation Drift (Implementation != Documentation)
`telegram_data\models\marts\core\dim_dates.sql`, `telegram_data\models\marts\core\schema.yml`, `telegram_data\models\staging\stg_image_detections.sql`, `telegram_data\models\staging\stg_telegram_messages.sql`, `telegram_data\tests\valid_message_timestamps.sql`

### Circular Dependencies
No circular dependencies detected.

## 📈 Recent Change Velocity (Hotspots)
- `docker-compose.yml` (0 commits in 90d)
- `.github\workflows\ci.yml` (0 commits in 90d)
- `api\crud.py` (0 commits in 90d)
- `api\database.py` (0 commits in 90d)
- `api\main.py` (0 commits in 90d)

## 📖 Module Purpose Index
### `docker-compose.yml`
- **Purpose**: This `docker-compose.yml` file defines a local development environment for a Python application with PostgreSQL integration. It orchestrates two services: a PostgreSQL database container (with persistent storage via a named volume) and an application container (built from the local codebase) that depends on the database, using environment variables for configuration. The setup enables seamless local testing by mounting the project directory into the app container and exposing the database port for external access.
- **Domain**: Analytics
### `.github\workflows\ci.yml`
- **Purpose**: This workflow automates Python CI/CD for the `main` branch, triggering on pushes and pull requests. It checks out the code, sets up Python 3.12, installs dependencies from `requirements.txt`, and executes Python scripts directly (likely tests or entry points) without specifying commands or test frameworks. The logic enforces a basic build/test pipeline for Python projects.
- **Domain**: CodeCarto
### `api\crud.py`
- **Purpose**: This module provides analytics and search functionality for message data stored in a `FctMessage` table. It exposes methods to retrieve channel-level message activity trends (e.g., daily message counts), search messages by text content, and identify the most frequently occurring predefined medical/cosmetic-related keywords (e.g., "gloves," "ibuprofen") across all messages. The logic focuses on operational insights for monitoring communication patterns and keyword prevalence.
- **Domain**: TelegramInsights
### `api\database.py`
- **Purpose**: This module establishes a SQLAlchemy database connection infrastructure, configuring a local session factory (`SessionLocal`) and declarative base (`Base`) for ORM operations. It abstracts database engine creation (using environment variables for the connection URL) and provides the foundational setup for defining and interacting with database models. The implementation ensures thread-safe session management via `autocommit=False` and `autoflush=False` while binding to a pre-configured SQLAlchemy engine.
- **Domain**: Analytics
### `api\main.py`
- **Purpose**: This module implements a FastAPI backend service exposing three REST endpoints for channel activity analytics, message search, and product popularity reporting. It retrieves structured activity data (e.g., user interactions) for a specified channel, enables full-text message search with results formatted as `SearchResult`, and provides ranked keyword counts (e.g., top products) via SQLAlchemy-backed CRUD operations. The design emphasizes database abstraction and dependency injection for session management.
- **Domain**: TelegramInsights
### `api\models.py`
- **Purpose**: This module defines a SQLAlchemy ORM schema for a Telegram message analytics data warehouse, implementing fact and dimension tables to track message metadata (e.g., sender, content, views) alongside normalized channel and date dimensions. It supports time-series analysis of Telegram messages by linking raw message data (`FctMessage`) to structured channel (`DimChannel`) and temporal (`DimDate`) references for reporting and BI purposes.
- **Domain**: TelegramInsights
### `api\schemas.py`
- **Purpose**: This module defines Pydantic data models (`BaseModel` subclasses) for structuring API response schemas related to message metadata, channel analytics, and search functionality. It encapsulates domain-specific data like message content (with IDs, timestamps, and view counts), channel activity statistics (daily message counts), and keyword-based search results, enabling type-safe serialization/deserialization of database/ORM objects for RESTful endpoints.
- **Domain**: TelegramInsights
### `runs\detect\train7\args.yaml`
- **Purpose**: This configuration file defines a customized object detection training pipeline for the YOLOv8-nano model (`yolov8n.pt`) on a medical pills dataset (`medical-pills.yaml`). It specifies hyperparameters (e.g., batch size 16, 100 epochs, CPU/AMP training, mosaic augmentation, and validation split) to train a detection model for pill classification, with emphasis on visualization (`plots: true`), label display (`showlabels: true`), and overlap mask augmentation (`overlapmask: true`) for improved generalization.
- **Domain**: PillVision
### `scripts\data_scraping.py`
- **Purpose**: This module is a Telegram channel scraper that asynchronously retrieves new messages from specified pharmaceutical/health-related channels, stores them in structured JSON format with metadata (sender ID, timestamp, views), and downloads associated media (photos) to a dated directory. It avoids reprocessing previously scraped messages by tracking message IDs across all historical scrapes, ensuring incremental updates while maintaining a deduplicated message archive.
- **Domain**: TelegramInsights
### `scripts\image_detection.py`
- **Purpose**: This module processes JPEG images stored in `data/raw/telegrammessages` to detect objects using two YOLOv8 models (a general-purpose model and a specialized "pill" model). It logs detections (object class, confidence, and bounding boxes) in a JSON file while also saving annotated images with visual markers, skipping previously processed files to avoid redundant work.
- **Domain**: PillVision
### `scripts\load_detected_objects.py`
- **Purpose**: This module ingests JSON-formatted detection records (likely from an object detection pipeline) into a PostgreSQL database. It reads structured detection data (e.g., message IDs, object classes, confidence scores, and metadata) from a file and inserts them into a database table, handling errors and transactions to ensure data integrity. The script assumes the JSON contains processed detection events with timestamps and associated image references.
- **Domain**: PillVision
### `scripts\load_raw_data.py`
- **Purpose**: This module ingests raw JSON-formatted Telegram message data from a structured directory hierarchy (organized by scrape date and channel) into a PostgreSQL database. It validates file paths, checks for duplicates, enriches each message with metadata (scrape date, channel name, and source path), and bulk-loads the data into a `raw` schema table. The workflow is designed to process large volumes of scraped data efficiently while avoiding reprocessing existing records.
- **Domain**: TelegramInsights
### `telegram_data\dbt_project.yml`
- **Purpose**: This `dbtproject.yml` defines a Telegram Data Transformation (dbt) project that organizes data pipelines into a staging layer (for raw/cleansed data) and a marts layer (business-focused analytical models) under the `core` schema. It specifies a source database (`shippingdb`) and raw schema (`raw`) for raw data ingestion, while enforcing a view-based materialization for staging models to ensure flexibility and performance control.
- **Domain**: Analytics
### `telegram_data\models\marts\core\dim_channels.sql`
- **Purpose**: This module creates a materialized table (`dimchannels`) that serves as a dimension table for Telegram channels by extracting unique channel names from the staging layer (`stgtelegrammessages`). It acts as a lookup table for channel identifiers (aliased as `channelid`) to enable efficient filtering, aggregation, or relationship joins in downstream analytics (e.g., channel-level metrics, user engagement by channel).
- **Domain**: TelegramInsights
### `telegram_data\models\marts\core\dim_dates.sql`
- **Purpose**: This module creates a dimension table (`dimdates`) that extracts and standardizes date attributes from Telegram message timestamps. It generates a structured reference table with granular date components (year, month, day, weekday name, month name, and a boolean flag for weekdays) to enable time-based analytics on message activity patterns. The table is materialized as a table to optimize repeated date lookups in downstream mart transformations.
- **Domain**: TelegramInsights
### `telegram_data\models\marts\core\fct_image_detections.sql`
- **Purpose**: This module creates a materialized table (`fctimagedetections`) that aggregates image detection metadata (object class, confidence score, timestamps, and file paths) from the staging layer (`stgimagedetections`) and enriches it with message-level context by joining to the `fctmessages` fact table. It serves as a core analytical dataset for analyzing object detection results in the context of Telegram messages, enabling downstream queries on detection performance, image metadata, and message associations.
- **Domain**: PillVision
### `telegram_data\models\marts\core\fct_messages.sql`
- **Purpose**: This module creates a materialized table (`mart.fctmessages`) that serves as a fact table for Telegram message analytics, aggregating raw message data (including metadata like timestamps, sender IDs, channel names, text content, and engagement metrics such as view counts and image flags) from the staging layer (`stgtelegrammessages`). It preserves all original fields while standardizing the `channelname` as `channelid` for downstream consistency, enabling time-series analysis of message volume, content, and user interactions.
- **Domain**: TelegramInsights
### `telegram_data\models\marts\core\schema.yml`
- **Purpose**: This module defines a star schema for analyzing Telegram message data, structuring facts (message-level metrics) in `fctmessages` (linked to `dimchannels` for channel context and `dimdates` for temporal analysis) and dimension tables (`dimchannels` and `dimdates`) to enable granular reporting on message volume, channel activity, and time-based trends. The schema enforces uniqueness constraints on primary keys (e.g., `messageid`, `channelid`, `date`) to ensure data integrity for downstream analytics.
- **Domain**: TelegramInsights
### `telegram_data\models\staging\schema.yml`
- **Purpose**: This schema defines a staging pipeline for processing Telegram message data and associated image-based object detections. It captures raw message metadata (timestamps, view counts, and image flags) in `stgtelegrammessages` and links YOLO-generated object detections (class labels, confidence scores, and image paths) to their source messages via foreign keys in `stgimagedetections`, enabling downstream analysis of content and visual patterns.
- **Domain**: TelegramInsights
### `telegram_data\models\staging\sources.yml`
- **Purpose**: This module defines a staging area configuration for a data pipeline that ingests raw Telegram message data (in JSON format) into a database schema (`raw`). It specifies a table (`telegrammessages`) to store unprocessed message payloads (including metadata like `scrapedate`, `channelname`, and `loadedat`), alongside a placeholder table (`imagedetections`) for potential future processing (e.g., extracting image-related features). The schema version (`2`) indicates this is part of a structured data lineage for transformation pipelines.
- **Domain**: TelegramInsights
### `telegram_data\models\staging\stg_image_detections.sql`
- **Purpose**: This module creates a materialized view (`stgimagedetections`) in the `staging` schema that exposes structured detection data from raw image processing results. It extracts key metadata (e.g., detection IDs, object classes, confidence scores, and timestamps) from the `raw.imagedetections` source to enable downstream analysis or transformation, acting as a curated staging layer for object detection pipelines.
- **Domain**: PillVision
### `telegram_data\models\staging\stg_telegram_messages.sql`
- **Purpose**: This module creates a materialized view (`stgtelegrammessages`) that extracts and transforms raw Telegram message data (stored as JSON in `raw.telegrammessages`) into a structured format. It standardizes fields like `messageid`, `messagetext`, `senderid`, and `viewcount`, while also deriving metadata (e.g., `hasimage`, `messagelength`) and timestamp conversions for downstream analytics. The view ensures consistency and accessibility for further processing in the staging layer.
- **Domain**: TelegramInsights
### `telegram_data\tests\positive_view_counts.sql`
- **Purpose**: This query identifies and flags Telegram messages in the staging layer (`stgtelegrammessages`) where the `viewcount` is negative, likely to detect data quality issues (e.g., invalid or corrupted view count values). It serves as a validation check to ensure no negative view counts exist in the dataset, which could indicate processing errors or malformed data.
- **Domain**: TelegramInsights
### `telegram_data\tests\valid_message_timestamps.sql`
- **Purpose**: This query identifies Telegram messages with invalid timestamps by filtering records from the staging table where timestamps are either pre-dating January 1, 2020 (likely an arbitrary cutoff for historical plausibility) or exceeding the current timestamp plus one day (flagging future timestamps as impossible). It serves as a data quality check to detect anomalous or malformed timestamp values in the message dataset.
- **Domain**: TelegramInsights
### `telegram_pipeline\pipelines\telegram_pipeline.py`
- **Purpose**: This module defines a Telegram data processing pipeline that automates the end-to-end workflow of scraping Telegram data, enriching it with object detection (YOLO), loading raw and detected data into PostgreSQL, and applying dbt transformations. The pipeline is scheduled to run daily at 11:15 PM (UTC) via Dagster, orchestrating external scripts for each step while passing outputs between operations.
- **Domain**: TelegramInsights
### `telegram_pipeline\pipelines\__init__.py`
- **Purpose**: This module (`telegrampipeline/pipelines/init.py`) serves as the entry point for Telegram-specific data processing pipelines, dynamically importing and registering pipeline configurations (e.g., `TelegramPipeline`) to handle Telegram-related data ingestion, transformation, or export workflows. It likely acts as a factory or dispatcher to instantiate the correct pipeline class based on configuration or environment settings, ensuring modular pipeline management for Telegram integrations.
- **Domain**: TelegramInsights
